# KSP Crime Intelligence Platform — Python (data + analytics/ML service)
# Data generation uses only the standard library (no deps required).
# The following are for the analytics/ML service (backend/appsail_ml).

pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1          # IsolationForest, DBSCAN, risk models
networkx==3.3                # criminal network / link analysis, Louvain communities
python-louvain==0.16         # community detection
scipy==1.13.1
fastapi==0.111.0             # analytics API (runs on Catalyst AppSail)
uvicorn[standard]==0.30.1
statsmodels==0.14.2          # temporal decomposition / trend baselines
joblib>=1.3                  # model persistence / registry artefacts

# ---- MLOps training pipeline (backend/ml) ----
mlflow>=3.0                  # experiment tracking (optional; pipeline degrades gracefully)
xgboost>=2.0                 # gradient boosting w/ optional CUDA GPU (device="cuda")

