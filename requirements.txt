# Runtime / serving dependencies — Catalyst AppSail installs THIS file (baked into the OCI image).
# Versions are pinned to EXACTLY the stack the champion models were trained/pickled with, so
# joblib.load() deserializes them correctly (numpy/sklearn pickles are version-sensitive — e.g.
# numpy's MT19937 random-state format changed across major versions).
pandas==2.3.3
numpy==2.4.1
scikit-learn==1.7.2          # champions (LogReg / Ridge / MLP / IsolationForest) + TF-IDF
scipy==1.16.3
networkx==3.6                # criminal network / link analysis, Louvain communities
python-louvain==0.16         # community detection
joblib==1.5.2                # model persistence / registry artefacts
fastapi==0.111.0             # analytics API (runs on Catalyst AppSail)
uvicorn[standard]==0.30.1
