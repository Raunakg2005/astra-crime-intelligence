{
  "stack": "python_3_11",
  "build_path": ".",
  "command": "sh -c '(echo PWD=$(pwd); id; df -h .; echo ===INSTALL===; python3 -m pip install --no-cache-dir --target \"$(pwd)/.deps\" -r requirements.txt) > diag.txt 2>&1 & python3 -m http.server ${X_ZOHO_CATALYST_LISTEN_PORT}'",
  "memory": 1024,
  "env_variables": {
    "PYTHONIOENCODING": "utf-8"
  }
}