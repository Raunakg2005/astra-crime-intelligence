# Dev / CI dependencies (installed on top of requirements.txt)
pytest==8.2.2
httpx==0.27.0          # FastAPI TestClient
ruff==0.5.5            # fast linter (bug-focused rules in CI)
pre-commit==3.7.1      # local git hooks
